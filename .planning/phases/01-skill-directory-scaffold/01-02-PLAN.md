---
phase: 01-skill-directory-scaffold
plan: 02
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - backend/skills/README.md
  - backend/skills/cross-channel-dedup/SKILL.md
  - backend/skills/cross-channel-dedup/input_schema.py
  - backend/skills/cross-channel-dedup/output_schema.py
autonomous: true
requirements:
  - SKILL-01
must_haves:
  truths:
    - "backend/skills/ directory exists alongside backend/app/, backend/tests/, backend/alembic/"
    - "backend/skills/cross-channel-dedup/ contains SKILL.md, input_schema.py, output_schema.py and no __init__.py"
    - "SKILL.md has a YAML front matter block at the top containing name and description fields, followed by all 5 required sections"
    - "input_schema.py exports CrossChannelDedupInput(BaseModel) with pass body; imports only pydantic"
    - "output_schema.py exports CrossChannelDedupOutput(BaseModel) with pass body; imports only pydantic"
    - "backend/skills/README.md explains kebab-case naming, required files, and how to add a new skill"
    - "All 10 tests in test_skill_scaffold.py pass after this plan executes"
  artifacts:
    - path: "backend/skills/README.md"
      provides: "Convention documentation for skill directory (satisfies SC-3)"
      contains: "kebab-case"
    - path: "backend/skills/cross-channel-dedup/SKILL.md"
      provides: "Skill documentation with YAML front matter and all required sections"
      contains: "System Prompt section"
    - path: "backend/skills/cross-channel-dedup/input_schema.py"
      provides: "Pydantic input schema skeleton"
      exports:
        - "CrossChannelDedupInput"
    - path: "backend/skills/cross-channel-dedup/output_schema.py"
      provides: "Pydantic output schema skeleton"
      exports:
        - "CrossChannelDedupOutput"
  key_links:
    - from: "backend/skills/cross-channel-dedup/input_schema.py"
      to: "pydantic.BaseModel"
      via: "from pydantic import BaseModel"
      pattern: "CrossChannelDedupInput(BaseModel)"
    - from: "backend/skills/cross-channel-dedup/output_schema.py"
      to: "pydantic.BaseModel"
      via: "from pydantic import BaseModel"
      pattern: "CrossChannelDedupOutput(BaseModel)"
    - from: "backend/tests/test_skill_scaffold.py"
      to: "backend/skills/cross-channel-dedup/"
      via: "SKILLS_DIR / SKILL_NAME path assertions"
      pattern: "SKILLS_DIR / SKILL_NAME"
---

<objective>
Create the backend/skills/ directory scaffold with README.md and the cross-channel-dedup
skill skeleton. This is the TDD green phase — all 10 tests from Plan 01 must pass after
this plan executes.

Purpose: Establish the on-disk skill directory convention that Phase 2's loader and Phase
4's dedup skill design will build on. No existing Python code is modified.

Output: 4 new files across backend/skills/. No __init__.py files. No app.* imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skill-directory-scaffold/01-CONTEXT.md
@.planning/phases/01-skill-directory-scaffold/01-RESEARCH.md
@.planning/phases/01-skill-directory-scaffold/01-VALIDATION.md
@.planning/phases/01-skill-directory-scaffold/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns the executor needs. -->

Existing backend layout (files that must NOT be touched):
  backend/
  ├── app/              (DO NOT TOUCH - per D-06)
  ├── tests/
  │   └── test_skill_scaffold.py  (written in Wave 0)
  ├── alembic/
  ├── pyproject.toml
  └── uv.lock

Target layout after this plan:
  backend/
  └── skills/
      ├── README.md
      └── cross-channel-dedup/
          ├── SKILL.md
          ├── input_schema.py
          └── output_schema.py
          (NO __init__.py — skill dirs are resource dirs, not Python packages)

Pydantic version in use: 2.12.5 (verified in backend venv)
Test command: cd backend && uv run pytest tests/test_skill_scaffold.py -v
Full suite: cd backend && uv run pytest tests/ -v
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create skills/ directory with README.md</name>
  <files>backend/skills/README.md</files>
  <read_first>
    - backend/tests/test_skill_scaffold.py (understand what the tests verify — README presence is checked manually via ls, not by pytest)
    - .planning/phases/01-skill-directory-scaffold/01-CONTEXT.md (decisions D-03, D-05, D-06 — scope boundaries)
  </read_first>
  <behavior>
    - README.md exists at backend/skills/README.md
    - README.md contains the word "kebab-case" (naming convention)
    - README.md contains a section explaining the three required files per skill
    - README.md contains instructions for adding a new skill without modifying existing code
    - README.md contains the SKILL.md format specification including the System Prompt section note
    - README.md contains schema file rules (stdlib + pydantic only, no app.* imports)
  </behavior>
  <action>
Create backend/skills/README.md. The file must contain the following content exactly
(no fenced code blocks with triple-dash sequences inside the README itself):

Title: "# Skills Directory"

Opening paragraph: Each subdirectory in `backend/skills/` is a skill — a self-contained LLM
capability definition that can be loaded and invoked by the skill framework.

Section "## Structure":
A table with three rows:
- SKILL.md | Documentation: YAML front matter + sections
- input_schema.py | Pydantic BaseModel subclass defining the skill's input
- output_schema.py | Pydantic BaseModel subclass defining the skill's output

Section "## Adding a New Skill":
Numbered list:
1. Create a new subdirectory under backend/skills/ using kebab-case naming.
2. Create all three required files (SKILL.md, input_schema.py, output_schema.py).
3. Do not modify any existing code — the skill loader discovers skills automatically.

Section "## Naming Conventions":
- Directory name: kebab-case — e.g., cross-channel-dedup
- Schema class names: PascalCase of directory name + Input/Output suffix
  - cross-channel-dedup → CrossChannelDedupInput, CrossChannelDedupOutput

Section "## SKILL.md Format":
Explain that SKILL.md must start with a YAML front matter block (triple-dash delimiters),
containing at minimum name (kebab-case) and description fields.
List the required sections: Purpose, System Prompt, Input, Output, Usage Example.
Note that the System Prompt section contains the LLM system prompt inside a fenced code block.

Section "## Schema File Rules":
- Import ONLY from stdlib and pydantic — no from app.* imports
- Export exactly one class per file named SkillNameInput / SkillNameOutput
- Use pass as placeholder body until the skill is fully designed

Do NOT create backend/skills/__init__.py. The skills/ directory is a resource directory,
not a Python package.

After writing, verify:
  ls /home/pica/assets/githubs/beancount-bot/backend/skills/README.md
  grep "kebab-case" /home/pica/assets/githubs/beancount-bot/backend/skills/README.md
  grep "System Prompt" /home/pica/assets/githubs/beancount-bot/backend/skills/README.md
  ls /home/pica/assets/githubs/beancount-bot/backend/skills/__init__.py 2>/dev/null || echo "GOOD: no __init__.py"
  </action>
  <verify>
    <automated>ls /home/pica/assets/githubs/beancount-bot/backend/skills/README.md && grep -c "kebab-case" /home/pica/assets/githubs/beancount-bot/backend/skills/README.md</automated>
  </verify>
  <acceptance_criteria>
    - ls backend/skills/README.md exits 0
    - grep "kebab-case" backend/skills/README.md exits 0 (word appears at least once)
    - grep "System Prompt" backend/skills/README.md exits 0
    - grep "from app\." backend/skills/README.md exits 1 (no app import instructions in the README)
    - ls backend/skills/__init__.py exits non-zero (no __init__.py created)
  </acceptance_criteria>
  <done>backend/skills/README.md created with all convention documentation, no __init__.py present</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create cross-channel-dedup skill skeleton (SKILL.md + input_schema.py + output_schema.py)</name>
  <files>
    backend/skills/cross-channel-dedup/SKILL.md
    backend/skills/cross-channel-dedup/input_schema.py
    backend/skills/cross-channel-dedup/output_schema.py
  </files>
  <read_first>
    - backend/tests/test_skill_scaffold.py (exact class names and assertions tested — CrossChannelDedupInput, CrossChannelDedupOutput, YAML front matter structure, 5 required sections)
    - .planning/phases/01-skill-directory-scaffold/01-CONTEXT.md (D-01 through D-06 locked decisions)
    - .planning/phases/01-skill-directory-scaffold/01-RESEARCH.md (Pitfall 3: empty class needs pass; Pitfall 4: no app.* imports; Pitfall 2: all 5 sections required)
  </read_first>
  <behavior>
    - SKILL.md first line is exactly "---" (YAML front matter open delimiter)
    - SKILL.md contains "name: cross-channel-dedup" in the front matter block
    - SKILL.md contains "description:" in the front matter block
    - SKILL.md front matter closes with "---" on its own line before the first section header
    - SKILL.md contains all 5 section headers: "## Purpose", "## System Prompt", "## Input", "## Output", "## Usage Example"
    - The "## System Prompt" section contains a fenced code block with a TODO placeholder for the LLM prompt
    - input_schema.py first non-comment import is: from pydantic import BaseModel
    - input_schema.py class definition: class CrossChannelDedupInput(BaseModel):
    - input_schema.py class body: pass (placeholder)
    - input_schema.py has no "from app." or "import app." anywhere
    - output_schema.py first non-comment import is: from pydantic import BaseModel
    - output_schema.py class definition: class CrossChannelDedupOutput(BaseModel):
    - output_schema.py class body: pass (placeholder)
    - output_schema.py has no "from app." or "import app." anywhere
    - cross-channel-dedup/ directory does NOT contain __init__.py
  </behavior>
  <action>
Create three files in backend/skills/cross-channel-dedup/. The exact required content
for each file is specified below. Exact class names, section headers, and import
statements are contractually required — the test file checks them by name.

FILE 1 — backend/skills/cross-channel-dedup/SKILL.md

The YAML front matter block must be the very first content of the file:

  (line 1)  ---
  (line 2)  name: cross-channel-dedup
  (line 3)  description: Identify suspected duplicate transactions across multiple payment channels using LLM semantic comparison grouped by weekly time windows.
  (line 4)  ---

Then a blank line, then the sections in this order:

  ## Purpose

  Cross-channel deduplication detects cases where the same underlying payment appears in
  multiple bill exports — for example, a WeChat Pay deduction that also shows up as a CMB
  bank card charge. The skill compares already-imported transactions against newly uploaded
  transactions, grouped into 7-day sliding windows, and returns suspected duplicate pairs.

  Used by: the import pipeline before classification (Phase 5 gate).

  ## System Prompt

  [fenced code block containing:]
  TODO (Phase 4): Add the LLM system prompt here.
  [end fenced code block]

  ## Input

  Defined in `input_schema.py` — see `CrossChannelDedupInput`.

  TODO (Phase 4): Describe the input fields once the schema is finalized.

  ## Output

  Defined in `output_schema.py` — see `CrossChannelDedupOutput`.

  TODO (Phase 4): Describe the output fields once the schema is finalized.

  ## Usage Example

  TODO (Phase 4): Add a code snippet showing how to invoke this skill.

FILE 2 — backend/skills/cross-channel-dedup/input_schema.py

  """
  Input schema for the cross-channel-dedup skill.

  Imported by the Phase 2 loader via:
      from skills.cross_channel_dedup.input_schema import CrossChannelDedupInput
  """
  from pydantic import BaseModel


  class CrossChannelDedupInput(BaseModel):
      # TODO (Phase 4): Add fields once the dedup skill design is complete.
      # Expected fields: existing_transactions (list), new_transactions (list),
      # window_days (int, default 7)
      pass

FILE 3 — backend/skills/cross-channel-dedup/output_schema.py

  """
  Output schema for the cross-channel-dedup skill.

  Imported by the Phase 2 loader via:
      from skills.cross_channel_dedup.output_schema import CrossChannelDedupOutput
  """
  from pydantic import BaseModel


  class CrossChannelDedupOutput(BaseModel):
      # TODO (Phase 4): Add fields once the dedup skill design is complete.
      # Expected fields: suspected_pairs (list), window_count (int)
      pass

Do NOT create __init__.py inside cross-channel-dedup/.

After creating the three files, run the full test suite to confirm all 10 tests pass:
  cd /home/pica/assets/githubs/beancount-bot/backend && uv run pytest tests/test_skill_scaffold.py -v

If any test fails, re-read the specific failing test assertion in test_skill_scaffold.py
and fix the exact string it is checking in the corresponding file.
  </action>
  <verify>
    <automated>cd /home/pica/assets/githubs/beancount-bot/backend && uv run pytest tests/test_skill_scaffold.py -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ls backend/skills/cross-channel-dedup/SKILL.md exits 0
    - ls backend/skills/cross-channel-dedup/input_schema.py exits 0
    - ls backend/skills/cross-channel-dedup/output_schema.py exits 0
    - ls backend/skills/cross-channel-dedup/__init__.py exits non-zero (no __init__.py)
    - head -1 backend/skills/cross-channel-dedup/SKILL.md outputs exactly "---"
    - grep "name: cross-channel-dedup" backend/skills/cross-channel-dedup/SKILL.md exits 0
    - grep "## System Prompt" backend/skills/cross-channel-dedup/SKILL.md exits 0
    - grep "## Usage Example" backend/skills/cross-channel-dedup/SKILL.md exits 0
    - grep "class CrossChannelDedupInput(BaseModel):" backend/skills/cross-channel-dedup/input_schema.py exits 0
    - grep "class CrossChannelDedupOutput(BaseModel):" backend/skills/cross-channel-dedup/output_schema.py exits 0
    - grep "from app\." backend/skills/cross-channel-dedup/input_schema.py exits 1 (no match)
    - grep "from app\." backend/skills/cross-channel-dedup/output_schema.py exits 1 (no match)
    - cd backend && uv run pytest tests/test_skill_scaffold.py -v shows 10 passed, 0 failed
    - cd backend && uv run pytest tests/ -v exits 0 (full suite still passes)
  </acceptance_criteria>
  <done>All 10 test_skill_scaffold.py tests pass; full test suite green; skill skeleton committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 1 creates only static files (Markdown, Python schema stubs). No user input, no API endpoints, no database operations, no LLM calls, no credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | input_schema.py / output_schema.py | accept | Schema stubs import only pydantic; no app.* imports means no circular import attack surface; files have no executable logic beyond class declaration |
| T-01-03 | Information Disclosure | SKILL.md | accept | No secrets or credentials in TODO placeholder content; system prompt is intentionally empty |
</threat_model>

<verification>
Run after both tasks complete:

  cd backend && uv run pytest tests/test_skill_scaffold.py -v
  Expected: 10 passed, 0 failed

  cd backend && uv run pytest tests/ -v
  Expected: all existing tests plus 10 new tests pass

  ls backend/skills/
  Expected: README.md  cross-channel-dedup/

  ls backend/skills/cross-channel-dedup/
  Expected: SKILL.md  input_schema.py  output_schema.py (no __init__.py)
</verification>

<success_criteria>
- backend/skills/ directory exists alongside backend/app/, backend/alembic/, backend/tests/
- backend/skills/README.md explains the kebab-case naming convention and three required files
- backend/skills/cross-channel-dedup/ contains SKILL.md, input_schema.py, output_schema.py
- No __init__.py in backend/skills/ or backend/skills/cross-channel-dedup/
- CrossChannelDedupInput and CrossChannelDedupOutput are importable via importlib.util (no app.* imports)
- SKILL.md has YAML front matter and all 5 sections including System Prompt
- All 10 tests in test_skill_scaffold.py pass
- Full backend test suite passes (cd backend && uv run pytest tests/ -v exits 0)
- No existing files in backend/app/ were modified (per D-06)
</success_criteria>

<output>
After completion, create .planning/phases/01-skill-directory-scaffold/01-02-SUMMARY.md
</output>
